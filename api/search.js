import * as searchIndex from '../lib/providers/search-index.js';
import * as meta from '../lib/providers/meta.js';
const MAX_RESULTS=60;
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const q=String(req.query?.q||'').trim();const limit=Math.min(Math.max(Number(req.query?.limit||20),1),MAX_RESULTS);if(!q)return res.status(400).json({error:'Query is required'});try{const providers=[searchIndex];if(meta.available())providers.unshift(meta);const outputs=await Promise.all(providers.map(async provider=>{try{return{provider:provider.name,...(await provider.discover({query:q,limit,maxPages:2}))};}catch(error){return{provider:provider.name,results:[],pagesFetched:0,error:error.message||'Provider failed'};}}));const seen=new Set(),results=[];let pagesFetched=0,timedOut=false;const searchedQueries=[],errors=[];for(const output of outputs){pagesFetched+=output.pagesFetched||0;timedOut=timedOut||Boolean(output.timedOut);searchedQueries.push(...(output.searchedQueries||[]).map(x=>typeof x==='string'?{query:x,provider:output.provider}:{...x,provider:output.provider}));if(output.error)errors.push({provider:output.provider,error:output.error});for(const item of output.results||[]){const key=String(item.url||'').replace(/\/$/,'').toLowerCase();if(!key||seen.has(key))continue;seen.add(key);results.push(item);}}const limitedResults=results.slice(0,limit);res.setHeader('Cache-Control','s-maxage=60, stale-while-revalidate=300');return res.status(200).json({query:q,count:limitedResults.length,totalFound:results.length,pagesFetched,timedOut,providers:outputs.map(x=>({name:x.provider,available:x.available!==false,error:x.error||null})),searchedQueries,errors,results:limitedResults});}catch(error){console.error('instagram crawler error',error);return res.status(502).json({error:'Unable to reach discovery providers right now.'});}}
